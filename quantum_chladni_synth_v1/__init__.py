"""Quantum-conditioned Laplace--Beltrami modal synthesizer.

The runtime module is intentionally not imported here so ``python -m`` can
start the OSC controller without preloading it through the package.
"""
